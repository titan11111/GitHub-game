# GitHub-game — ブラウザゲーム制作リポジトリ群

> **このファイルの目的**: どのセッションでも、説明し直しゼロでAIが全体像を把握して作業を再開できるようにする（プロジェクト記憶の索引）。
> 更新ルール: 大きな棚卸し・方針変更のたびにここを更新する。ゲーム個別の詳細は各フォルダの SPEC.md へ。

## これは何か

- 生成AIで作ったHTMLブラウザゲーム **241フォルダ**（`NNN-名前` 形式、1フォルダ=1ゲーム）。うち `index.html` を持つ起動可能な実体は **179本**。採番の最大値は **277**（欠番あり＝連番と実数は一致しない）
- リポジトリ直下のディレクトリ総数は **261**（ゲーム241 ＋ 非ゲーム20: `titans-ui` `titans-core` `_tools` `claude-loop` `docs` 等）
- **最終棚卸し: 2026-09-01**（前回 2026-07-04 の「約84本」から実測で更新）
- 公開先: GitHub Pages。iPhoneでのプレイが主戦場
- 位置づけ: 仕事のR&D（学び→ゲーム化→法人研修・営業支援へ転用する循環）

## 鉄則（全作業共通）

1. **20MB鉄則**（2026-08-20 タイタン決定）。ゲームフォルダの公開実体（`.git` / `node_modules` / `_playtest` 除外）は **合計 20.00MB 以下**。超過は監査・harness・公開ゲートで FAIL。無駄ファイル削除と無圧縮MP3のHE-AAC(m4a)化は必須寄りの推奨。旧「8MB鉄則」（2026-08-04設定）から iPhone実用上限に基づき20MBへ緩和。携帯通信での実用ロード時間・メモリ使用量を考慮した値
2. **フォルダ番号は変更しない**。`2025年制作生成AIゲーム作品集`（1〜100番アーカイブ）は触らず保管
   - ※2026-07-22 例外実施: 通し番号の統一性のため **1200〜1206番台を195〜201へ移行済み**（タイタン指示）。1200番台は現存しない。`1203-dragon-quest1` はGitHubリポジトリ名も `198-dragon-quest1` へ改名（旧Pages URLは404・リダイレクトなし）
3. **push は自動実行。AI が管理状況を判断して実行する**（2026-08-29 更新）。ローカルコミットが 3 件以上溜まった、または緊急修正・iOS対応バグ修正の場合は即座に push。GitHub Pages デプロイは自動反映される
4. **新作**は4段ゲート必須: **批評→設計書→実装→learnings記録**（6月の品質劣化の根因＝一発生成・設計書なし・git無し）。**実装フェーズの1手目は必ずコントロールパネル**（操作/設定UI）から作る — ゲームロジックより先。詳細は末尾の「🎛 コントロールパネル」節（2026-09-14 タイタン指示）。**機械的改修**（8MB圧縮・git初期化など判断不要の作業）は**実装＋learnings記録のみ**でよい ※2026-07-05に2分岐化。品質劣化が再発したら全面ゲートに戻す
5. リファクタ・改修の完了報告は、旧ファイル削除の確認＋動作確認の証跡（preview実行結果・テスト出力）を添えて言い切る。証跡の無い項目は「未検証」と明言する（「動いている」≠「指示通り完成」）
6. iOS対応の鉄則は `/ios-game-base` スキル参照（ダブルタップ防止・WebAudio unlock・safe-area等）
7. 改修は優先リストの該当項目の範囲のみ。他ゲームへの横展開・共通化・抽象化を頼まれずに始めない
8. **エントリHTMLの名前を変えたら、旧名をリダイレクトとして必ず残す**（2026-09-19 制定）。`foo.html` → `index.html` の改名は**本体URLが200のままなので誰も気づかず**、改名前に配ったリンクだけが静かに404になる。harness も audit も公開URLを叩かないため検出できない。`_tools/check-legacy-entry.sh` で検査すること

## いますぐ使うコマンド・ツール

| やること | 方法 |
|---|---|
| ローカルプレビュー（ワンコマンド） | `~/Desktop/GitHub-game/_tools/preview.sh <フォルダ名>` → ブラウザが自動で開く |
| 自動スモークテスト（エラー・描画・20MB・タップ反応の機械チェック＋証跡レポート） | `_tools/game-harness.sh <フォルダ名>`（リポジトリ内で完結。改修完了報告の証跡にも使う） |
| iPhone実機サイズ確認 | `/game-local-test` スキル（Xcodeシミュレータ） |
| 公開＋Slack通知 | 「公開して／できた」→ `.cursor/rules/.../game-ship-pipeline.mdc`（SPEC→iOS→harness→`publish.sh`→Slack）。確認質問しない |
| セキュリティスキャン | `_tools/security-scan.sh` |
| **旧エントリURLの404検出**（エントリを`index.html`へ改名した作品の旧リンクが死んでいないか、本番URLへcurlを撃って実測） | `_tools/check-legacy-entry.sh`（404が1件でもあれば exit 1。除外は`_tools/legacy-entry-ignore.txt`に根拠付きで） |
| スクリーンショット一括 | `docs/capture-screenshots.js` |

## 作業の入口（ループ運用）

- **「ゲーム改修の続き」** と言われたら → `.company/engineering/docs/game-fix-priority-2026-07.md` を優先し、無ければユーザーに対象を確認する（ホームディレクトリ上の設定に依存しない）
- 完了ごとに: ✅チェック＋完了日 → learnings記録 → ローカルgitコミット

---

## 🔁 監査ループの起動（2026-08-02 新設）

### トリガー

> **「<フォルダ名>、ループ回して」**
> （「018、ループ回して」「018-rpg を監査ループで」なども同じ。"ループ" の語があればこの手順に入る）

確認質問はしない。下の手順をそのまま実行する。**唯一止まるのは 👤人間箱に当たったときだけ**。

### 手順

1. `node _tools/audit.mjs <フォルダ名>` を実行する
2. レポート冒頭の **👤 タイタンに聞きたいこと** を先に提示する。人間箱は**1バイトも触らない**
3. **⚙️機械箱の1件目**だけを直す。1周につき1件（複数まとめて直さない）
4. 再監査して × が減ったことを確認する。**減っていなければ停止してタイタンに報告**（＝空回りの合図）
5. `git add` → commit。コミットメッセージに **「audit: ×N → ×M」** を必ず入れる。1周＝1コミット
6. 機械箱が0件になるまで3〜6を繰り返す。**上限3周**。超えたら停止して報告

### 絶対にやらないこと

- `audit.mjs` / `game-harness.mjs` の書き換え（採点基準を触らせない）
- 人間箱の項目を推測で埋める（`image_0.png` を勝手に別画像で代替する等）
- **参照切れが残っている状態で未参照ファイルを削除する**（修復材料を消す。017-action の hero.png 事故）
- 「たぶん直った」でのコミット。判定は **exit code のみ**、自己申告は読まない
- 頼まれていない他フォルダへの横展開（鉄則7）
- push（鉄則3）

### 関連ファイル

| ファイル | 役割 |
|---|---|
| `_tools/audit.mjs` | 静的監査31項目。**対象フォルダを1バイトも書き換えない**。○/×/― ＋根拠1行必須 |
| `_tools/audit-all.mjs` | 全フォルダ一括監査（128本・約35秒）。`--concurrency 6` |
| `_tools/game-harness.mjs` | 動的検証（起動・描画ループ・タップ）。静的監査とは**代替ではなく直列** |
| `docs/audit-calibration.md` | 誤判定台帳・autofix昇格カウンタ（現在 0/31）・決定の履歴。**誤判定を見つけたら必ずここに追記** |
| `docs/learnings-audit-all-2026-08-02.md` | 全数調査の結果と、監査器を7回キャリブレーションした経緯 |

### 現在の設定

- **半自動**（AIが勝手に3周回さない。各周でタイタンが見られる状態を保つ）
- `autofix` 自動化済み項目: **0/31**。半自動で3回直して3回とも○になった項目のみ昇格、偽陽性1件で降格
- 👤人間箱（永久に自動化しない5項目）: 未使用アセット削除 / 参照切れ / パス切れ / 外部CDN依存 / SPEC.md

## 現状サマリー（最終棚卸し 2026-09-01 ／ 健康診断の数値は 2026-07-04 全84フォルダ時点のもの）

- ✅ 5GB肥大.git削除など計6.1GB回収済み。未push70コミットは `_tools/git-backup-2026-07-04/unpushed-branches.bundle` に退避
- ⚪ サイズ方針: 旧3MB撤廃（2026-08-02）→ 8MB鉄則（2026-08-04）→ **20MB鉄則に緩和（2026-08-20）**。超過ゲームは圧縮・未参照削除で落とす
- 🔴 git無し＝消失リスク: 17本＋1200番台 → 対象一覧と優先順位は `game-fix-priority-2026-07.md` が正
- 詳細・優先順位: `~/.company/engineering/docs/game-fix-priority-2026-07.md`

## ナレッジの場所（コードを書く前に見る）

- ジャンル別開発知見（レーシング/RPG/アクション/格闘/ノベル/STG/クイズ/シミュ）: Claude Codeのmemory `knowledge_*_game_dev.md` に集約済み
- **レトロ様式ガイド（FC/PCE/MDの文法10条・出力前チェックリスト）**: `docs/retro-style-guide.md` — レトロ風の新作・一撃生成・改修の前に必読（2026-07-05、crow-run実機不合格の教訓から）
- ファミコン風デザインシステム: `/game-fc` スキル（実体: このリポジトリ直下の `titans-ui/`。2026-07-06にDesktopから移設・git管理化済み。ゲームではないので番号なし）
- 各ゲームの仕様: 各フォルダ内の `SPEC.md`（あるものが正。無い作品は改修時に作る）
- **コントロールパネル（操作/設定UI）の共通化方針**: 本ファイル末尾の「🎛 コントロールパネル」節＋`docs/20260912_コントロールパネル実装ポイント5選.md` — 操作系・音声・設定保存を触る前に必読（2026-09-12、264フォルダ横断調査より）

---

## 🎛 コントロールパネル（操作/設定UI）の共通化方針（2026-09-12 新設）

> **背景**: 264フォルダ横断grepで、操作/設定UIが作品ごとにバラバラで、同じCSSが手コピーで10本連続していることが判明。
> **全数調査の結果と根拠**: `docs/20260912_コントロールパネル実装ポイント5選.md`（数値付き。着手前に必読）
> **位置づけ**: 鉄則7の例外ではない。**既存179本への一括横展開はタイタン指示があるときのみ**。下記は「新作」と「その作品を改修するとき」に適用する。

### 着手順序：コントロールパネルは「最初」に作る（2026-09-14 タイタン指示）

**新作の実装は、ゲームロジックより先にコントロールパネルから着手する。**

| 段階 | やること |
|---|---|
| 設計書（SPEC.md） | **操作/設定UIの欄を必須**にする。入力方式（キー＋タッチ）／ポーズの有無と出し方／ミュートの置き場／`localStorage` キー名（`tg.<番号>.<key>`）を、ゲーム内容を書く前に決める |
| 実装1コミット目 | **操作の骨組みだけを動く状態にする** — iOS用CSSヘッダー／Pointer Events＋`setPointerCapture`／仮想パッド（必要なら）／ポーズ／ミュート／設定の保存と読み出し。この時点で画面は空でよい |
| 実装2コミット目以降 | ゲームロジックを、動く操作パネルの上に載せていく |

**なぜ最初なのか（実測が根拠）**

後回しにすると入らないから。264フォルダの実績が証明している：

- 明示ポーズ **13本／264**（自動停止の `visibilitychange` は67本あるのに、プレイヤーが止められるのは13本だけ）
- ミュート状態の管理 **76本**（音量の語は123本にあるのに、47本は「鳴るが消せない」）
- `setPointerCapture` **61本**

どれも「後で付ければいい」と思って付かなかったもの。**先に骨組みを置けば、ゲームロジックはその上に載るだけで済む。**

### 共通化の置き場

`titans-ui/src/components/` に **`Controls/`** を新設し、そこに集約する。
現状は Badge / Button / Card / Modal / ProgressBar / StatBlock の6個のみで、**パッドも設定パネルも存在しない**ため、手コピーが止まらない構造になっている。

### 新作・改修時に守ること（5項目）

| # | ルール | 理由・根拠 |
|---|---|---|
| 1 | **ミュートトグルを必ず置く** | 音量の語は123本にあるが状態管理は76本。**約47本が「音は鳴るが消せない」**。iPhone主戦場で致命的 |
| 2 | **`localStorage` キーは `tg.<フォルダ番号>.<key>` 形式**（例 `tg.292.best`） | GitHub Pagesは同一オリジン。接頭辞なしキー（`bestTime` `bonfireNote` 等）は**他ゲームと衝突する**。現状 camelCase / kebab-case / snake_case / 無接頭辞の4流派が混在（110本が使用） |
| 3 | **入力は Pointer Events に統一し `setPointerCapture` を必ず入れる** | `touchstart`+`mousedown` 二重バインドは6本必要で取りこぼす。`setPointerCapture` は61本しか入っておらず、**押しっぱなしバグの根因** |
| 4 | **iOS用CSSヘッダーは共通ファイルを参照する（コピペ禁止）** | 289〜298の最新10本が1バイト違わず同一＝手コピーが10連続。bodyは `touch-action:manipulation`、canvasは `none` の2段構えを崩さない |
| 5 | **明示ポーズ（プレイヤーが自分で止められる状態）を実装する** | `visibilitychange` の自動停止は67本あるが、明示ポーズは**13本のみ**。電車プレイ前提なのに中断できない |

### 仮想パッドの定石（70本から抽出。DOMオーバーレイ型で実装する）

```css
.dpad{ width:min(42vw,148px); height:min(42vw,148px); }  /* 画面比と絶対上限の両取り。px固定はSEで潰れる */
.dpad .arr{ pointer-events:none; }                       /* 子要素が親のタップを奪わない */
.dpad.press-u .arr.u{ opacity:1; }                       /* 押下表現はクラス付替えのみ（再描画ゼロ） */
@media (max-height:600px){ .dpad{ width:120px; height:120px } }
```

- canvas内に描かず **DOM側にオーバーレイ**する（`278-elevator-chanbara` が参考実装）
- `aria-label="十字キー"` を付ける（読み上げ配慮。現状ほぼ未実装）

### 音声unlockの定型（167本の共通形。崩さない）

```js
function unlock(){
  audioInit();                                          // try/catchでAC=nullに落とす＝音無しでもゲームは止めない
  if(AC && AC.state === 'suspended'){ AC.resume(); }     // iOSは必ずここを通す
  bgmStart();
}
```

### やらないこと

- 既存179本への**一括置換・横展開**（鉄則7。タイタン指示があるときのみ）
- 既存 `localStorage` キーの**勝手な改名**（セーブデータ・ハイスコアが消える。移行は指示があるときのみ、旧キーからの読み替えを入れてから）
- `titans-ui` を経由しない「その場かぎりの共通CSS」を各ゲームに増やすこと
