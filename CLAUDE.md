# GitHub-game — ブラウザゲーム制作リポジトリ群

> **このファイルの目的**: どのセッションでも、説明し直しゼロでAIが全体像を把握して作業を再開できるようにする（プロジェクト記憶の索引）。
> 更新ルール: 大きな棚卸し・方針変更のたびにここを更新する。ゲーム個別の詳細は各フォルダの SPEC.md へ。

## これは何か

- 生成AIで作ったHTMLブラウザゲーム約84本（`NNN-名前` 形式のフォルダ、1フォルダ=1ゲーム）
- 公開先: GitHub Pages。iPhoneでのプレイが主戦場
- 位置づけ: 仕事のR&D（学び→ゲーム化→法人研修・営業支援へ転用する循環）

## 鉄則（全作業共通）

1. **20MB鉄則**（2026-08-20 タイタン決定）。ゲームフォルダの公開実体（`.git` / `node_modules` / `_playtest` 除外）は **合計 20.00MB 以下**。超過は監査・harness・公開ゲートで FAIL。無駄ファイル削除と無圧縮MP3のHE-AAC(m4a)化は必須寄りの推奨。旧「8MB鉄則」（2026-08-04設定）から iPhone実用上限に基づき20MBへ緩和。携帯通信での実用ロード時間・メモリ使用量を考慮した値
2. **フォルダ番号は変更しない**。`2025年制作生成AIゲーム作品集`（1〜100番アーカイブ）は触らず保管
   - ※2026-07-22 例外実施: 通し番号の統一性のため **1200〜1206番台を195〜201へ移行済み**（タイタン指示）。1200番台は現存しない。`1203-dragon-quest1` はGitHubリポジトリ名も `198-dragon-quest1` へ改名（旧Pages URLは404・リダイレクトなし）
3. **push はタイタンの指示があるまでしない**（ローカルコミットまで）。GitHub公開側は触らない
4. **新作**は4段ゲート必須: **批評→設計書→実装→learnings記録**（6月の品質劣化の根因＝一発生成・設計書なし・git無し）。**機械的改修**（8MB圧縮・git初期化など判断不要の作業）は**実装＋learnings記録のみ**でよい ※2026-07-05に2分岐化。品質劣化が再発したら全面ゲートに戻す
5. リファクタ・改修の完了報告は、旧ファイル削除の確認＋動作確認の証跡（preview実行結果・テスト出力）を添えて言い切る。証跡の無い項目は「未検証」と明言する（「動いている」≠「指示通り完成」）
6. iOS対応の鉄則は `/ios-game-base` スキル参照（ダブルタップ防止・WebAudio unlock・safe-area等）
7. 改修は優先リストの該当項目の範囲のみ。他ゲームへの横展開・共通化・抽象化を頼まれずに始めない

## いますぐ使うコマンド・ツール

| やること | 方法 |
|---|---|
| ローカルプレビュー（ワンコマンド） | `~/Desktop/GitHub-game/_tools/preview.sh <フォルダ名>` → ブラウザが自動で開く |
| 自動スモークテスト（エラー・描画・20MB・タップ反応の機械チェック＋証跡レポート） | `_tools/game-harness.sh <フォルダ名>`（リポジトリ内で完結。改修完了報告の証跡にも使う） |
| iPhone実機サイズ確認 | `/game-local-test` スキル（Xcodeシミュレータ） |
| 公開＋Slack通知 | 「公開して／できた」→ `.cursor/rules/.../game-ship-pipeline.mdc`（SPEC→iOS→harness→`publish.sh`→Slack）。確認質問しない |
| セキュリティスキャン | `_tools/security-scan.sh` |
| スクリーンショット一括 | `docs/capture-screenshots.js` |

## 作業の入口（ループ運用）

- **「ゲーム改修の続き」** と言われたら → `.company/engineering/docs/game-fix-priority-2026-07.md` を優先し、無ければユーザーに対象を確認する（ホームディレクトリ上の設定に依存しない）
- 完了ごとに: ✅チェック＋完了日 → learnings記録 → ローカルgitコミット

---

## 🔁 監査ループの起動（2026-08-02 新設）

### トリガー

> **「<フォルダ名>、ループ回して」**
> （「018、ループ回して」「018-rpg を監査ループで」なども同じ。"ループ" の語があればこの手順に入る）

確認質問はしない。下の手順をそのまま実行する。**唯一止まるのは 👤人間箱に当たったときだけ**。

### 手順

1. `node _tools/audit.mjs <フォルダ名>` を実行する
2. レポート冒頭の **👤 タイタンに聞きたいこと** を先に提示する。人間箱は**1バイトも触らない**
3. **⚙️機械箱の1件目**だけを直す。1周につき1件（複数まとめて直さない）
4. 再監査して × が減ったことを確認する。**減っていなければ停止してタイタンに報告**（＝空回りの合図）
5. `git add` → commit。コミットメッセージに **「audit: ×N → ×M」** を必ず入れる。1周＝1コミット
6. 機械箱が0件になるまで3〜6を繰り返す。**上限3周**。超えたら停止して報告

### 絶対にやらないこと

- `audit.mjs` / `game-harness.mjs` の書き換え（採点基準を触らせない）
- 人間箱の項目を推測で埋める（`image_0.png` を勝手に別画像で代替する等）
- **参照切れが残っている状態で未参照ファイルを削除する**（修復材料を消す。017-action の hero.png 事故）
- 「たぶん直った」でのコミット。判定は **exit code のみ**、自己申告は読まない
- 頼まれていない他フォルダへの横展開（鉄則7）
- push（鉄則3）

### 関連ファイル

| ファイル | 役割 |
|---|---|
| `_tools/audit.mjs` | 静的監査31項目。**対象フォルダを1バイトも書き換えない**。○/×/― ＋根拠1行必須 |
| `_tools/audit-all.mjs` | 全フォルダ一括監査（128本・約35秒）。`--concurrency 6` |
| `_tools/game-harness.mjs` | 動的検証（起動・描画ループ・タップ）。静的監査とは**代替ではなく直列** |
| `docs/audit-calibration.md` | 誤判定台帳・autofix昇格カウンタ（現在 0/31）・決定の履歴。**誤判定を見つけたら必ずここに追記** |
| `docs/learnings-audit-all-2026-08-02.md` | 全数調査の結果と、監査器を7回キャリブレーションした経緯 |

### 現在の設定

- **半自動**（AIが勝手に3周回さない。各周でタイタンが見られる状態を保つ）
- `autofix` 自動化済み項目: **0/31**。半自動で3回直して3回とも○になった項目のみ昇格、偽陽性1件で降格
- 👤人間箱（永久に自動化しない5項目）: 未使用アセット削除 / 参照切れ / パス切れ / 外部CDN依存 / SPEC.md

## 現状サマリー（2026-07-04 全84フォルダ健康診断より）

- ✅ 5GB肥大.git削除など計6.1GB回収済み。未push70コミットは `_tools/git-backup-2026-07-04/unpushed-branches.bundle` に退避
- ⚪ サイズ方針: 旧3MB撤廃（2026-08-02）→ 8MB鉄則（2026-08-04）→ **20MB鉄則に緩和（2026-08-20）**。超過ゲームは圧縮・未参照削除で落とす
- 🔴 git無し＝消失リスク: 17本＋1200番台 → 対象一覧と優先順位は `game-fix-priority-2026-07.md` が正
- 詳細・優先順位: `~/.company/engineering/docs/game-fix-priority-2026-07.md`

## ナレッジの場所（コードを書く前に見る）

- ジャンル別開発知見（レーシング/RPG/アクション/格闘/ノベル/STG/クイズ/シミュ）: Claude Codeのmemory `knowledge_*_game_dev.md` に集約済み
- **レトロ様式ガイド（FC/PCE/MDの文法10条・出力前チェックリスト）**: `docs/retro-style-guide.md` — レトロ風の新作・一撃生成・改修の前に必読（2026-07-05、crow-run実機不合格の教訓から）
- ファミコン風デザインシステム: `/game-fc` スキル（実体: このリポジトリ直下の `titans-ui/`。2026-07-06にDesktopから移設・git管理化済み。ゲームではないので番号なし）
- 各ゲームの仕様: 各フォルダ内の `SPEC.md`（あるものが正。無い作品は改修時に作る）
