# コード監査レポート: 193-titan-land

- 判定: **コード監査上の問題あり**（○ 22 / × 4 / ― 7）
- × の内訳: **機械箱 3件**（ループが直す） / **人間箱 1件**（タイタンが決める）
- 実行: 2026-08-02T08:04:23.253Z
- ツール: `_tools/audit.mjs`（静的監査・対象フォルダは無変更）

> この監査は静的解析のみに基づく。実機でのプレイ挙動は検証していない。
> 動的検証は `_tools/game-harness.mjs`、実機確認は人間の責務。

# 👤 タイタンに聞きたいこと（1件）

## 1. 外部CDN依存 1件 — どうする？

**いま起きてること**

- `https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap`

**質問** — ローカルに同梱する／そのまま外部参照でいく／使うのをやめる、どれ？

**答えたらこうする** — 同梱ならフォントファイルを取得して参照を書き換え → 再監査

*見た目に直結するので自動判断しない*

---

# 監査明細

## 1. 実装確認

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| 必要なファイルが存在する（index.html） | ○ | ⚙️ | index.html を検出（41595バイト） |
| スタイルとスクリプトが実装されている | ○ | ⚙️ | CSS: 0ファイル＋インライン<style>あり / JS: 0ファイル＋インライン<script>1件 |
| 未使用アセットがない | ○ | 👤 | アセット0件すべてコード内から参照を検出 |
| 読み込まれていないJS/CSSがない | ― | ⚙️ | JS/CSSファイルが0件（全てインライン）のため対象外 |
| 重複コードがない（同名function重複定義） | ○ | ⚙️ | トップレベルfunction宣言 49件、同名重複0件 |

## 2. 構文確認

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| HTMLエラーなし（タグ開閉・id重複） | ○ | ⚙️ | HTML 1ファイル: 主要7タグの開閉一致・id重複0件 |
| CSS構文エラーなし（括弧・コメント） | ○ | ⚙️ | CSS 0ファイル＋インライン: {} 対応一致・コメント閉じ漏れ0件 |
| JavaScript構文エラーなし（node --check） | ○ | ⚙️ | 1スクリプトを node --check でパース、全件成功 |

## 3. 依存関係確認

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| 読み込み順が正しい | ― | ⚙️ | index.html に外部scriptタグが0件（インライン実装） |
| 参照切れがない（HTML/CSS/JSの全参照） | ○ | 👤 | ローカル参照 0件すべて実ファイルに解決（外部URL 1件は対象外） |
| 画像・音声・SVGのパス切れがない | ― | 👤 | アセット参照が0件のため対象外 |
| 外部CDN依存 | × | 👤 | 外部URL 1件を検出（オフライン/障害時に破綻）: https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap |

## 4. 既存機能への影響確認（回帰）

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| 未コミットの変更がない（差分の追跡可能性） | × | ⚙️ | 未コミット2件: M SPEC.md, ?? learnings.md |
| コンソールエラーが増えていない | ― | ⚙️ | 過去レポート1件あり。静的監査では動的エラーを測れないため harness の結果で判定すること |
| 新たな警告が発生していない | ― | ⚙️ | 静的監査ではランタイム警告を観測できない（harness側の責務） |

## 5. iOS適合確認

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| viewport設定（device-width＋拡大禁止） | ○ | ⚙️ | content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" |
| touch-action | ○ | ⚙️ | CSSに touch-action 宣言 1件、うち none/manipulation 1件 |
| user-select / -webkit-user-select | ○ | ⚙️ | user-select 宣言 2件（-webkit-プレフィクス: あり） |
| タップハイライト・長押しメニュー抑止 | ○ | ⚙️ | -webkit-tap-highlight-color:transparent = あり / -webkit-touch-callout:none = あり |
| ダブルタップズーム防止（touchend/touchmove） | × | ⚙️ | touchendハンドラ: なし / touchmove preventDefault: なし |
| safe-area対応 | ○ | ⚙️ | env(safe-area-inset-*) を 3箇所で使用 |
| スクロール・バウンス防止 | ○ | ⚙️ | html/body に overflow:hidden または position:fixed を検出 |
| BGM開始処理（ユーザージェスチャー内でunlock） | ○ | ⚙️ | AudioContext: あり / ジェスチャーイベント: あり / resume()orサイレントバッファ: あり |
| BGMループ設定 | × | ⚙️ | ループ指定を検出できない（1回鳴って終わる） |
| コントローラー配置（オンスクリーン操作） | ○ | ⚙️ | 操作系のid/class を 2件検出（touchstart/pointerdownハンドラ: あり） |
| position・z-indexが競合していない | ○ | ⚙️ | z-index 宣言 1件、同値3回以上の重複なし（最大値 9999） |

## 6. 品質確認

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| アセット総量（参考値・合否対象外） | ― | ⚙️ | 合計 0.05MB（4ファイル）／重い順: index.html 0.04MB, SPEC.md 0.01MB, learnings.md 0.00MB |
| 不要なデバッグ出力が残っていない | ○ | ⚙️ | console.log/debug/info の呼び出し0件 |
| 命名規則が統一されている（camelCase） | ○ | ⚙️ | function 49件中 camelCase 49件（100%）／snake_case 0件 |
| ファイル構成が標準要件通り（サブフォルダは images/ audio/ のみ） | ○ | ⚙️ | 直下のみのフラット構成（サブフォルダ0件） |
| SPEC.md が存在する（合格条件の定義元） | ○ | 👤 | SPEC.md を検出（6781バイト） |

## 7. 最終チェック

| 項目 | 判定 | 箱 | 根拠 |
|---|:---:|:---:|---|
| 全項目に根拠1行が付いている | ○ | ⚙️ | 31項目すべてに根拠テキストを出力 |
| Obsidianへ記録済み | ― | ⚙️ | ObsidianのMCP接続が未認証のため、本ツールはローカルmdのみ出力する |

## ⚙️ 機械箱 — ループが直す（上から順に1周1件）

1. **未コミットの変更がない（差分の追跡可能性）**（4. 既存機能への影響確認（回帰）） — 未コミット2件: M SPEC.md, ?? learnings.md
2. **ダブルタップズーム防止（touchend/touchmove）**（5. iOS適合確認） — touchendハンドラ: なし / touchmove preventDefault: なし
3. **BGMループ設定**（5. iOS適合確認） — ループ指定を検出できない（1回鳴って終わる）
