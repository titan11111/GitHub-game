# コード監査レポート: 017-action

- 判定: **コード監査上の問題あり**（○ 18 / × 10 / ― 4）
- 実行: 2026-08-02T07:44:37.535Z
- ツール: `_tools/audit.mjs`（静的監査・対象フォルダは無変更）

> この監査は静的解析のみに基づく。実機でのプレイ挙動は検証していない。
> 動的検証は `_tools/game-harness.mjs`、実機確認は人間の責務。

## 1. 実装確認

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| 必要なファイルが存在する（index.html） | ○ | index.html を検出（39486バイト） |
| スタイルとスクリプトが実装されている | ○ | CSS: 1ファイル＋インライン<style>あり / JS: 1ファイル＋インライン<script>1件 |
| 未使用アセットがない | × | 未参照6件 / 計7.32MB: audio/run.mp3, enemy7.png, enemy9.png, hero.png, summon_3.png, summon_5.png |
| 重複コードがない（同名function重複定義） | × | 重複16件: init×2, updateUI×2, setupEventListeners×2, handleMessageButton×2, startGame×2 |

## 2. 構文確認

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| HTMLエラーなし（タグ開閉・id重複） | ○ | HTML 1ファイル: 主要7タグの開閉一致・id重複0件 |
| CSS構文エラーなし（括弧・コメント） | ○ | CSS 1ファイル＋インライン: {} 対応一致・コメント閉じ漏れ0件 |
| JavaScript構文エラーなし（node --check） | ○ | 2スクリプトを node --check でパース、全件成功 |

## 3. 依存関係確認

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| 読み込み順が正しい | ― | index.html に外部scriptタグが0件（インライン実装） |
| 参照切れがない（HTML/CSS/JSの全参照） | × | 切れ3件: image_0.png(←index.html), image_1.png(←index.html), image_3.png(←index.html) |
| 画像・音声・SVGのパス切れがない | × | 切れ3件: image_0.png, image_1.png, image_3.png |
| 外部CDN依存 | × | 外部URL 3件を検出（オフライン/障害時に破綻）: https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap, https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap |

## 4. 既存機能への影響確認（回帰）

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| 未コミットの変更がない（差分の追跡可能性） | ○ | git status --porcelain の出力0行＝作業ツリーはHEADと一致 |
| コンソールエラーが増えていない | ― | docs/harness-reports に 017-action の過去レポートが0件＝比較基準なし（harness実行が必要） |
| 新たな警告が発生していない | ― | 静的監査ではランタイム警告を観測できない（harness側の責務） |

## 5. iOS適合確認

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| viewport設定（device-width＋拡大禁止） | ○ | content="width=device-width, initial-scale=1.0, user-scalable=no, maximum-scale=1.0" |
| touch-action | ○ | CSSに touch-action 宣言 2件、うち none/manipulation 2件 |
| user-select / -webkit-user-select | ○ | user-select 宣言 4件（-webkit-プレフィクス: あり） |
| タップハイライト・長押しメニュー抑止 | ○ | -webkit-tap-highlight-color:transparent = あり / -webkit-touch-callout:none = あり |
| ダブルタップズーム防止（touchend/touchmove） | ○ | touchendハンドラ: あり / touchmove preventDefault: なし |
| safe-area対応 | × | env(safe-area-inset-*) をCSSに検出できない（ノッチ/ホームバーに被る） |
| スクロール・バウンス防止 | ○ | html/body に overflow:hidden または position:fixed を検出 |
| BGM開始処理（ユーザージェスチャー内でunlock） | × | AudioContext: あり / ジェスチャーイベント: あり / resume()orサイレントバッファ: なし |
| BGMループ設定 | ○ | .loop = true または <audio loop> を検出 |
| コントローラー配置（オンスクリーン操作） | ○ | 操作系のid/class を 12件検出（touchstart/pointerdownハンドラ: あり） |
| position・z-indexが競合していない | ○ | z-index 宣言 9件、同値3回以上の重複なし（最大値 20） |

## 6. 品質確認

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| モバイル3MB鉄則 | × | フォルダ内アセット合計 16.45MB（上限3.00MB）／最大 audio/Neon Circuitry.mp3 4.98MB, audio/run.mp3 3.28MB, enemy9.png 1.46MB |
| 不要なデバッグ出力が残っていない | × | console.log/debug/info 3件が残存 |
| 命名規則が統一されている（camelCase） | ○ | function 56件中 camelCase 56件（100%）／snake_case 0件 |
| ファイル構成が標準要件通り（サブフォルダは images/ audio/ のみ） | ○ | サブフォルダ 1件: audio |
| SPEC.md が存在する（合格条件の定義元） | × | SPEC.md が無い＝この監査は「壊れてないか」しか見られず、仕様通りかは検証不能 |

## 7. 最終チェック

| 項目 | 判定 | 根拠 |
|---|:---:|---|
| 全項目に根拠1行が付いている | ○ | 30項目すべてに根拠テキストを出力 |
| Obsidianへ記録済み | ― | ObsidianのMCP接続が未認証のため、本ツールはローカルmdのみ出力する |

## × 一覧（修正対象）

1. **未使用アセットがない**（1. 実装確認） — 未参照6件 / 計7.32MB: audio/run.mp3, enemy7.png, enemy9.png, hero.png, summon_3.png, summon_5.png
2. **重複コードがない（同名function重複定義）**（1. 実装確認） — 重複16件: init×2, updateUI×2, setupEventListeners×2, handleMessageButton×2, startGame×2
3. **参照切れがない（HTML/CSS/JSの全参照）**（3. 依存関係確認） — 切れ3件: image_0.png(←index.html), image_1.png(←index.html), image_3.png(←index.html)
4. **画像・音声・SVGのパス切れがない**（3. 依存関係確認） — 切れ3件: image_0.png, image_1.png, image_3.png
5. **外部CDN依存**（3. 依存関係確認） — 外部URL 3件を検出（オフライン/障害時に破綻）: https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap, https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap
6. **safe-area対応**（5. iOS適合確認） — env(safe-area-inset-*) をCSSに検出できない（ノッチ/ホームバーに被る）
7. **BGM開始処理（ユーザージェスチャー内でunlock）**（5. iOS適合確認） — AudioContext: あり / ジェスチャーイベント: あり / resume()orサイレントバッファ: なし
8. **モバイル3MB鉄則**（6. 品質確認） — フォルダ内アセット合計 16.45MB（上限3.00MB）／最大 audio/Neon Circuitry.mp3 4.98MB, audio/run.mp3 3.28MB, enemy9.png 1.46MB
9. **不要なデバッグ出力が残っていない**（6. 品質確認） — console.log/debug/info 3件が残存
10. **SPEC.md が存在する（合格条件の定義元）**（6. 品質確認） — SPEC.md が無い＝この監査は「壊れてないか」しか見られず、仕様通りかは検証不能
